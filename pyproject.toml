[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
# Start CI with correctness checks; style rules can be enabled incrementally
# without blocking the existing research scripts.
select = ["E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
